// src/shared/constants/roles.js

const ROLES = {
  CUSTOMER:
    "customer",

  BUSINESS:
    "business",

  ADMIN: "admin",
};

const ROLE_LIST =
  Object.values(
    ROLES
  );

module.exports = {
  ROLES,
  ROLE_LIST,
};